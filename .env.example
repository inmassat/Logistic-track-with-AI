# Copy this file to .env and add your key. .env is gitignored and never committed.
# Create a key at https://console.anthropic.com/settings/keys
ANTHROPIC_API_KEY=sk-ant-...

# Port the AI service listens on (the Vite dev server proxies /api here).
PORT=8787
